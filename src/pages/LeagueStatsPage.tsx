import { useEffect, useState } from 'react';
import { BarChart3, Plus, Save, X } from 'lucide-react';
import { supabase, type Standing } from '@/lib/supabase';

function LeagueStatsPage() {
  const [standings, setStandings] = useState<Standing[]>([]);
  const [editing, setEditing] = useState<Standing | null>(null);
  const [form, setForm] = useState({ team: '', played: 0, won: 0, drawn: 0, lost: 0, goals_for: 0, goals_against: 0, points: 0, season: '2025-2026' });
  const load = async () => { const { data } = await supabase.from('standings').select('*').order('points', { ascending: false }); if (data) setStandings(data); };
  useEffect(() => { void load(); }, []);
  const open = (row?: Standing) => { setEditing(row || null); setForm(row ? { team: row.team, played: row.played, won: row.won, drawn: row.drawn, lost: row.lost, goals_for: row.goals_for, goals_against: row.goals_against, points: row.points, season: row.season || '2025-2026' } : { team: '', played: 0, won: 0, drawn: 0, lost: 0, goals_for: 0, goals_against: 0, points: 0, season: '2025-2026' }); };
  const save = async () => { if (!form.team.trim()) return; const result = editing ? await supabase.from('standings').update(form).eq('id', editing.id) : await supabase.from('standings').insert(form); if (!result.error) { setEditing(null); await load(); } };
  const setNumber = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: Number(value) || 0 }));
  return <div className="space-y-6 animate-fade-in"><div className="flex flex-col sm:flex-row sm:items-end justify-between gap-4"><div><p className="text-sm text-blue-400 font-medium mb-2">COMPETITION OVERVIEW</p><h2 className="text-3xl font-bold tracking-tight">League Stats</h2><p className="text-slate-500 mt-2">Keep an eye on the table and team performance.</p></div><button onClick={() => open()} className="flex items-center gap-2 self-start px-3 py-2 rounded-lg bg-blue-500 text-white text-sm font-semibold hover:bg-blue-400"><Plus className="w-4 h-4" /> Add team</button></div><div className="bg-slate-900 border border-slate-800 rounded-xl overflow-hidden"><div className="px-5 py-4 border-b border-slate-800 flex items-center gap-3"><BarChart3 className="w-5 h-5 text-blue-400" /><div><h3 className="font-semibold">League table</h3><p className="text-xs text-slate-500 mt-1">Season 2025-2026 · {standings.length} teams</p></div></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b border-slate-800 text-xs text-slate-500 uppercase tracking-wider"><th className="text-left px-5 py-4 w-12">#</th><th className="text-left px-4 py-4">Team</th><th className="text-right px-4 py-4">P</th><th className="text-right px-4 py-4">W</th><th className="text-right px-4 py-4">D</th><th className="text-right px-4 py-4">L</th><th className="text-right px-4 py-4">GF</th><th className="text-right px-4 py-4">GA</th><th className="text-right px-4 py-4">GD</th><th className="text-right px-5 py-4">Pts</th><th></th></tr></thead><tbody>{standings.map((row, index) => <tr key={row.id} className="border-b border-slate-800/70 last:border-0 hover:bg-slate-800/30"><td className="px-5 py-4 text-slate-500">{index + 1}</td><td className="px-4 py-4 font-medium text-slate-200">{row.team}</td><td className="px-4 py-4 text-right text-slate-400">{row.played}</td><td className="px-4 py-4 text-right text-slate-400">{row.won}</td><td className="px-4 py-4 text-right text-slate-400">{row.drawn}</td><td className="px-4 py-4 text-right text-slate-400">{row.lost}</td><td className="px-4 py-4 text-right text-slate-400">{row.goals_for}</td><td className="px-4 py-4 text-right text-slate-400">{row.goals_against}</td><td className="px-4 py-4 text-right text-slate-400">{row.goals_for - row.goals_against}</td><td className="px-5 py-4 text-right font-bold text-blue-400">{row.points}</td><td className="px-3 py-4"><button onClick={() => open(row)} className="text-xs text-slate-500 hover:text-white">Edit</button></td></tr>)}</tbody></table>{!standings.length && <div className="py-16 text-center"><BarChart3 className="w-8 h-8 text-slate-700 mx-auto mb-3" /><p className="text-slate-500 text-sm">No league data yet</p></div>}</div></div>{editing !== null && <div className="fixed inset-0 z-50 bg-black/70 flex items-center justify-center p-4"><div className="bg-slate-900 border border-slate-700 rounded-2xl w-full max-w-xl"><div className="flex justify-between p-5 border-b border-slate-800"><h3 className="font-semibold">Edit league team</h3><button onClick={() => setEditing(null)}><X className="w-5 h-5 text-slate-500" /></button></div><div className="p-5 grid grid-cols-2 sm:grid-cols-3 gap-4"><label className="col-span-2 sm:col-span-3 text-xs text-slate-400">Team<input value={form.team} onChange={(e) => setForm({ ...form, team: e.target.value })} className="mt-1.5 w-full bg-white border border-slate-800 rounded-lg px-3 py-2.5 text-sm" /></label>{(['played', 'won', 'drawn', 'lost', 'goals_for', 'goals_against', 'points'] as (keyof typeof form)[]).map((key) => <label key={key} className="text-xs text-slate-400 capitalize">{key.replace('_', ' ')}<input type="number" value={form[key]} onChange={(e) => setNumber(key, e.target.value)} className="mt-1.5 w-full bg-white border border-slate-800 rounded-lg px-3 py-2.5 text-sm" /></label>)}</div><div className="flex justify-end gap-2 p-5 border-t border-slate-800"><button onClick={() => setEditing(null)} className="px-4 py-2 text-sm text-slate-400">Cancel</button><button onClick={save} className="flex items-center gap-2 px-4 py-2 rounded-lg bg-blue-500 text-white text-sm font-semibold"><Save className="w-4 h-4" /> Save</button></div></div></div>}</div>;
}
export default LeagueStatsPage;
